import { environments } from "./common/environments";

// cree variable de entorno para ser usada en app.module 
export const jwtConstants = {
  secret:'secretKey'//environments.jwt.jwtTkn,
};

// 
//'secretKey',